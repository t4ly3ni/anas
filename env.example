# Example environment variables
# PORT is set by the platform automatically; set locally if needed
PORT=8501

# If you use MLflow model registry or remote storage, specify URIs here
# MLFLOW_TRACKING_URI=file:./mlflow/mlruns
